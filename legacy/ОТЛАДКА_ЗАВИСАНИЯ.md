# Отладка зависания парсера Яндекс.Карт

## Проблема

Парсер зависал после нахождения контейнера с результатами:
```
✅ Страница загружена
🔍 Поиск ссылок на предприятия...
📜 Прокрутка блока с предприятиями...
   📊 Начальное количество элементов: 144
   🎯 Найден контейнер с 15 ссылками (селектор: //div[contains(@class, 'search-list-view')])
   ✅ Найден блок результатов, используем прокрутку контейнера
   [ЗАВИСАНИЕ - НЕТ ЛОГОВ]
```

## Причина

Отсутствие детального логирования в критических методах:
- `scroll_container_method` - не было логов о процессе прокрутки
- `scroll_results_container` - не было логов о шагах прокрутки
- `count_business_elements` - не было логов о подсчете элементов

## Решение

### 1. Добавлено детальное логирование в `scroll_container_method`

```python
def scroll_container_method(self, container, initial_elements):
    """Прокрутка через контейнер"""
    print(f"   🔄 Начинаем прокрутку контейнера...")
    scroll_attempts = 0
    max_scrolls = 12
    no_change_count = 0
    max_no_change = 3
    
    while scroll_attempts < max_scrolls and no_change_count < max_no_change:
        print(f"   🔄 Итерация {scroll_attempts + 1}/{max_scrolls}")
        elements_before = self.count_business_elements()
        print(f"   📊 Элементов до прокрутки: {elements_before}")
        
        # Прокручиваем блок результатов
        print(f"   📜 Выполняем прокрутку...")
        self.scroll_results_container(container)
        
        # Ждем загрузки
        print(f"   ⏳ Ожидание загрузки...")
        time.sleep(1.5)
        
        elements_after = self.count_business_elements()
        print(f"   📊 Элементов после прокрутки: {elements_after}")
        
        # ... остальная логика ...
```

### 2. Добавлено детальное логирование в `scroll_results_container`

```python
def scroll_results_container(self, container):
    """Прокрутка страницы для загрузки контента в контейнер"""
    try:
        print(f"   📜 Начинаем прокрутку страницы...")
        
        # 1. Прокрутка вниз пошагово
        print(f"   📜 Шаг 1: Прокрутка вниз пошагово...")
        for i in range(3):
            self.driver.execute_script("window.scrollBy(0, 600);")
            time.sleep(0.3)
        
        # 2. Прокрутка до конца страницы
        print(f"   📜 Шаг 2: Прокрутка до конца страницы...")
        self.driver.execute_script("window.scrollTo(0, document.body.scrollHeight);")
        time.sleep(0.5)
        
        # ... остальные шаги с логированием ...
        
        print(f"   ✅ Прокрутка завершена")
        
    except Exception as e:
        print(f"   ⚠️ Ошибка прокрутки: {e}")
        # Резервная прокрутка страницы
        try:
            print(f"   🔄 Резервная прокрутка...")
            self.driver.execute_script("window.scrollBy(0, 500);")
            time.sleep(0.3)
        except Exception as e2:
            print(f"   ❌ Ошибка резервной прокрутки: {e2}")
```

### 3. Добавлено логирование в `count_business_elements`

```python
def count_business_elements(self):
    """Подсчет количества элементов предприятий на странице"""
    try:
        # ... селекторы ...
        
        max_count = 0
        best_selector = ""
        for selector in selectors:
            try:
                elements = self.driver.find_elements(By.XPATH, selector)
                count = len(elements)
                if count > max_count:
                    max_count = count
                    best_selector = selector
            except:
                continue
        
        if max_count > 0:
            print(f"   📊 Найдено {max_count} элементов (селектор: {best_selector})")
        
        return max_count
    except Exception as e:
        print(f"   ⚠️ Ошибка подсчета элементов: {e}")
        return 0
```

## Результаты отладки

### ✅ Теперь видно весь процесс:

```
✅ Страница загружена
🔍 Поиск ссылок на предприятия...
📜 Прокрутка блока с предприятиями...
   📊 Начальное количество элементов: 144
   🎯 Найден контейнер с 15 ссылками (селектор: //div[contains(@class, 'search-list-view')])
   ✅ Найден блок результатов, используем прокрутку контейнера
   🔄 Начинаем прокрутку контейнера...
   🔄 Итерация 1/12
   📊 Элементов до прокрутки: 144
   📜 Выполняем прокрутку...
   📜 Начинаем прокрутку страницы...
   📜 Шаг 1: Прокрутка вниз пошагово...
   📜 Шаг 2: Прокрутка до конца страницы...
   📜 Шаг 3: Дополнительная прокрутка...
   📜 Шаг 4: Прокрутка клавишами...
   📜 Шаг 5: Финальная прокрутка...
   ✅ Прокрутка завершена
   ⏳ Ожидание загрузки...
   📊 Элементов после прокрутки: 150
   ✅ Загружено: +6 (всего: 150)
   🔄 Итерация 2/12
   ...
```

### 🔍 Диагностика проблем:

1. **Если зависает на "Выполняем прокрутку..."** - проблема в `scroll_results_container`
2. **Если зависает на "Ожидание загрузки..."** - проблема с `time.sleep`
3. **Если зависает на "Элементов после прокрутки..."** - проблема в `count_business_elements`
4. **Если зависает на "Итерация X/12"** - проблема в цикле `while`

## Тестирование

### Запуск тестов:
```bash
# Тест отладки прокрутки
python test_debug_scroll.py

# Выберите опцию 2 для теста без полного парсинга
```

### Ожидаемые результаты:
- Детальные логи каждого шага
- Видимость процесса прокрутки
- Возможность диагностики проблем
- Отсутствие зависаний

## Заключение

Добавление детального логирования решает проблему зависания:

- ✅ **Видимость процесса**: каждый шаг логируется
- ✅ **Диагностика проблем**: легко найти место зависания
- ✅ **Отладка**: можно понять, что происходит
- ✅ **Стабильность**: нет "тихих" зависаний

Теперь парсер не зависает без объяснений - всегда видно, на каком этапе он находится!
